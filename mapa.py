from rurple import cargar_mapa
class Mapa():
	def __init__ (self):
		#self.alto = len(lista_mapa[])
		#self.ancho = len(lista_mapa[0][0])
		#self.monedas = 


	def ancho ():
		an = len(lista_mapa[0][0])
		print (an)
	def alto ():
		alt = len(lista_mapa/an)
		print (alt)
